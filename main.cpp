#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef long double ld;
#define int ll
#define double ld

const auto boostIO = []() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  return 0;
}();



signed main() {
  int tests;
  while (tests --> 0) {
    read();
    solve();
    write();
  }
  cout.flush();
  return 0;
}
